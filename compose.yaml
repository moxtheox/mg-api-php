services:
  geotab-demo:
    image: cmox/mg-api-php-demo:latest
    build:
      context: .
      dockerfile: Dockerfile.bun
    env_file: .env
    ports:
      - "3000:3000"
    volumes:
      - geotab_sessions:/usr/src/app/sessions

volumes:
  geotab_sessions: